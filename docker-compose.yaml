version: "3"

services:
    nextjs:
        build:
            context: .
        ports:
            - "3000"
        container_name: nextjs
        restart: always
        networks:
            - overcraft

networks:
    overcraft:
        name: overcraft
