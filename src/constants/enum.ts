export enum PAGE_PATH {
    HOME = '/',
    DOCUMENTATION = '/documentation',
    BUILDS = '/builds',
    DASHBOARD = '/dashboard',
    SIGNIN = '/login',
    UPDATE = '/update',
    PROFILE = '/profile',
}
export enum VERBES_HTTP {
    GET = 'GET',
    POST = 'POST',
    PATCH = 'PATCH',
    DELETE = 'DELETE',
}

export enum MODULE_NESTJS {
    BUILD = '/builds',
    AUTH = '/auth',
    USER = '/user',
    STEP = '/step',
}
